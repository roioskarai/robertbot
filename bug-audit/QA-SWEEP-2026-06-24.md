# QA Bug-Sweep & Critical/High Fix Campaign — 2026-06-24

Run by `project-director` (orchestration) + `qa-operations` (Playwright sweep) + `security`
(re-audit) + `product-engineering` (fixes). Tested against local dev (`localhost`, real keys;
UI swept in demo mode for full page access). Baseline & post-fix: **tsc ✅ · lint ✅ · build ✅**.

## Headline
The 4 bugs the owner worried about were **already fixed** (verified live): FAQ editor
click-to-delete, one-click bot delete (now has a confirm dialog), forgot-password (sends a
real request), and the preview chat. Most prior security holes in the old audit are also
already fixed. This pass found a small, concrete Critical/High set — the code ones are fixed.

## Fixed now (code, verified green)
| # | Bug | Severity | Files |
|---|---|---|---|
| F1 | Plan bot-limit bypassable via `PUT /api/bots/[id]` and `connect-meta` (only `/activate` enforced it) | HIGH (security) | new `src/lib/bot-limit.ts`; `api/bots/[id]/route.ts`; `api/bots/[id]/connect-meta/route.ts`; `api/bots/[id]/activate/route.ts` (refactored) |
| F2 | `connect-meta` had no cross-tenant uniqueness check on `whatsapp_number` / `meta_phone_number_id` (number hijack / mis-route) | HIGH (security) | `api/bots/[id]/connect-meta/route.ts` (taken-guard + 23505 backstop) |
| F3 | Onboarding signup advanced even when the signup API failed | HIGH | `src/app/onboarding/page.tsx` (`doSignup` gated on `res.ok` + in-flight guard) |
| F4 | Onboarding `finish()` fired inside a `setCurStep` updater → duplicate bot POST | HIGH | `src/app/onboarding/page.tsx` (`finish()` moved out + `finishing` guard) |

## Confirmed but NOT a code fix — owner / gated action required
- **Homepage `<title>` is corrupted** → renders `Robert - ???? ????? ??????` (Hebrew letters
  stored as literal `?`). Confirmed real via Node fetch **and** a real Chromium `document.title`.
  Root cause = **corrupted DB data**: `pages.meta.metaTitle` for the `home` record (the source
  code/defaults are clean UTF-8 em-dash Hebrew). **Fix:** re-save the home page's SEO title in
  Admin → Site → Pages → home, OR an `APPROVED - DATABASE` UPDATE. Worth checking the home
  `metaDescription` too. SEO/brand impact on the most important page.
- **`meta_phone_number_id` unique index** — migration written but NOT run:
  `app/supabase/0006_meta_unique.sql` (idempotent partial-unique index). Needs `APPROVED - DATABASE`.
  (The app-level guard in F2 works without it; the index is the race-proof backstop.)

## By design (left unchanged)
- **Dashboard sidebar is always off-canvas (hamburger) on desktop too.** `dashboard.module.css`
  is a verbatim copy of the locked `robert-dashboard.html` design (no media queries by design).
  Not a bug. If a desktop sidebar is wanted, that's a design change via `brand-design` + the owner.

## Deferred to a later pass (Medium/Low — logged, not fixed)
- Inbox search field doesn't filter; History filter pills don't filter; Inbox reply isn't
  appended to the visible thread (no optimistic add). `dashboard/page.tsx` (~662, 703-735, 997-1010).
- Onboarding lets you skip all 5 steps with no validation (creates a default "הבוט שלי").
- Decorative drag-handle for services (no DnD); various toast-only buttons (account save,
  password, download invoice, notif bell, support, manual connect) — confirm wired in prod.
- `effectiveRole()` treats a null `admin_role` as `super_admin` (legacy) — verify no admin row
  has a null role (else silent code-injection rights). `src/lib/site/roles.ts`.

## Not covered (needs a separate pass)
- **Admin panel + CMS UI** (`/admin/*`) — behind admin auth + 2FA; demo mode does not bypass
  it. Needs a separate authenticated sweep with admin credentials.
- **Dependency CVEs:** `npm audit` = 6 (5 high, 1 moderate), almost all Next.js advisories whose
  only fix is a **major bump to next@16 (breaking)**; 14.2.35 is already the latest 14.2.x.
  Treat as a separate, owner-approved dependency-upgrade task (don't force mid-campaign).
- **Anthropic credit exhausted** — preview/live AI replies fall back locally; load credit before
  relying on the AI engine in production.

## Approvals needed to finish
- `APPROVED - DATABASE` — run `0006_meta_unique.sql` and/or fix the home `metaTitle`.
- `APPROVED FOR PRODUCTION DEPLOYMENT` — owner deploys after a green readiness gate.
- Release-readiness (QA): was **72/100**; the High code bugs are fixed — remaining pre-deploy
  blockers are the homepage title (DB) and the Anthropic credit, plus an admin-UI pass.
